const discord = require("discord.js")

module.exports = {
  name: "avatar",
  description: "get avatar someone",
  
  async run(client, message, args) {
    
    let member = message.mentions.first() || message.author
    
    let avatar
  }
  
}
